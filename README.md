# プログラミング学習記録　2025/11/25（火）

## 昨日やったこと
- PHP基礎学習（変数のスコープ）
 
## 今日やること
- PHP基礎学習（変数のスコープ・クラス）
  
## 明日やること
- PHP基礎学習（続き）

## メモ
- クラスとはモノ（オブジェクト）の設計図
- クラスを元にオブジェクトを作ることをインスタンスという
- クラスの作り方
```
// クラスを定義する
class クラス名 {
  クラスの特徴
}
```
- クラス名は慣習的に先頭を大文字にする
- インスタンス化する方法
```
// インスタンス化する
インスタンスを入れる変数名 = new クラス名();
```
- プロパティの書き方
```
class Product {
// プロパティを定義する
  pubric $name;
}
```
- プロパティとは、オブジェクトが持つデータ（関数）のこと
- ’pubric’はアクセス権を示すコード（クラス内外どこからでも）、'private'と記述するとクラス外からはアクセスできない。
- プロパティにアクセスして、値を代入したり出力する方法.（アロー演算子->を使う、先頭に$マークは不要）
```
<?php
class Product {
 public $name;
}

// インスタンス化する
$shampoo = new Product();

// プロパティにアクセスし、値を代入する
$shampoo->name = 'シャンプー';

// プロパティにアクセスし、値を出力する
echo $shampoo->name;
```
- メソッドとは、オブジェクトがもつ関数のこと（例：在庫数を更新する関数）
- メソッドへのアクセス方法（プロパティ同様にアロー演算子を使う）
```
<?php
class Product {
 private $name;
 publick fanction set_name(string $name) {
 $this->name = $name;
}
}
$shampoo = new Product;
//メソッドにアクセスして実行する
$shampoo->set_name('シャンプー');
?>
```


